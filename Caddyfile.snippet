# Вставить в основной Caddyfile домена на VPS.
#
# Приватность сайта держится на секретном пути — реальный слаг решается
# отдельно и в этот файл не попадает в открытом виде.

TODO-domain.example {
	reverse_proxy 127.0.0.1:8081
}
